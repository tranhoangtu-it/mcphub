name: clickup
package: "@taazkareem/clickup-mcp-server"
source: npm
description: "ClickUp project management — tasks, spaces, and lists via MCP"
category: productivity
tags: [clickup, project-management, tasks, agile]
author: taazkareem
verified: false
config:
  command: npx
  args: ["-y", "@taazkareem/clickup-mcp-server"]
  env:
    CLICKUP_API_KEY: ""
